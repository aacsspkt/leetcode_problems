// use crate::Solution;

// impl Solution {
//     pub fn is_match(s: String, p: String) -> bool {
//         if p.is_empty() {
//             return s.is_empty();
//         }

//         let first_match = !p.is_empty()
//             && (p[0..1].to_owned() == s[0..1].to_owned() || p[0..1].to_owned() == ".");

//         todo!();
//         // false
//     }
// }
